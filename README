MediaWiki Extension — SemanticHTML

SemanticHTML is a simple MediaWiki extension to add support for HTML4 phrase elements 
plus the HTML5 time & video elements to MediaWiki. By default, MediaWiki strips out these
descriptive elements.

This extension adds support for:

  * abbr
  * acronym
  * data
  * dfn
  * kbd
  * samp
  * time
  * var
  * video
  
Originally written for microformats.org/wiki, by Ben Ward

  * http://microformats.org/wiki/
  * http://ben-ward.co.uk

To Do:
  * consider support for object element to enable embedding of videos or images with fallback -tç
